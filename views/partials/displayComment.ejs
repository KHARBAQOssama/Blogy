
<div class="row d-flex justify-content-center" >
    <div class="col-md-12 col-lg-12">
      <div class="card shadow-0 border" style="background-color: #f0f2f5;">
        <div class="card-body p-4" >
         
          <div class="card mb-4" style="overflow: auto; max-height: 200px;">
            <div class="card-body">
              <p><%= comment.text %></p>
              <div class="d-flex justify-content-between">
                <div class="d-flex flex-row align-items-center">
                  <img src="https://mdbcdn.b-cdn.net/img/Photos/Avatars/img%20(4).webp" alt="avatar" width="25"
                    height="25" />
                  <p class="small mb-0 ms-2"><%= comment.user.full_name %></p>
                </div>
                <div class="d-flex gap-2 flex-row align-items-center">
                  <p class="small text-muted mb-0"><%= comment.createdAt.toDateString() %> <%= comment.createdAt.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) %>

                  </p>
                  <form action="/comment/<%= comment.id %>/delete" method="post">
                    <input type="hidden" class="hidden" value="<%= comment.id %>">
                    <button type="submit" class="border-0">
                      <i class="fas fa-trash-can" ></i>
                    </button>
                  </form>
                </div>
              </div>
            </div>
          </div>  
        </div>
      </div>
    </div>
  </div>